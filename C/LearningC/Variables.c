//Some basic pratice using variables

#include <stdio.h>

int main() {

    int firstSibling = 25;
    printf("The first child is %d years old \n", firstSibling);

    int secondSibling = firstSibling;
    printf("The second child is %d years old. \nThey are the same age!", secondSibling);

    return 0;

}